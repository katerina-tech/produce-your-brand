# Python
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Node / Next.js
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts

# Secrets
.env
.env.local

# Generated data (rebuildable: `uv run python scripts/build_index.py`)
backend/data/index/
backend/data/uploads/
*.db
*.db-shm
*.db-wal

# OS / editor
.DS_Store
Thumbs.db
desktop.ini

# Scraped website text from real companies.
#
# Kept locally, never published. Reading it to extract capabilities is text and
# data mining, which §44b UrhG permits; republishing 67 companies' own website
# copy in a public repository is redistribution, which it does not. The script
# that produces it is committed, so anybody can rebuild it themselves.
backend/data/company_pages.json
